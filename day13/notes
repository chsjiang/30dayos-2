refactor timer fifo and printf function, also slightly modify the counter to compare performance
also use one fifo for timer/keyboard/mouse signals, this makes performance way faster
finishing day13, use a toy linked list to manage timers